var msg;
var showMessage;

msg = "It's full of stars!";

showMessage = function () {
	console.log(msg);
};

showMessage();

var msg;
var showMessage;
var message;

msg = "It's full of stars!";
message = "Hello, world!";

showMessage = function () {
	console.log(msg);
};

showMessage();

var msg;
var showMessage;
var message;

msg = "It's full of stars!";
message = "Hello, world!";

showMessage = function () {
	console.log(msg);
	console.log(message);
};

showMessage();

var msg;
var showMessage;
var message;

msg = "It's full of stars!";
message = "Hello, world!";

showMessage = function () {
	console.log(msg);
	console.log(message);
	console.log(msg + " " + message);
};

showMessage();
