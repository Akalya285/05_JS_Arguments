var showMessage;

showMessage = function (message) {
	console.log("The message is: " + message + " Have a great day!");
};

// 1) Changed the message argument
showMessage("Hello, world!");

// 2) Added two more calls with different arguments
showMessage("JavaScript is awesome!");
showMessage("Keep on coding!");

// 3) Changed the showMessage function itself to add extra text after the message
